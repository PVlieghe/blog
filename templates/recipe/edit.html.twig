{% extends 'base.html.twig' %}


{% block body %}
<div class="body">
    <div class="container mt-5">
        <div class="card custom-card bg-white rounded p-4 border border-primary border-2">
            <h1 class="card-title">Modifiez votre recette: "{{ recipe.name}}"</h1>
            
            <div class="card-body">
                {{ include('recipe/_form.html.twig', {'button_label': 'Update'}) }}

                <a href="{{ path('app_recipe_index') }}" class="btn btn-danger mt-3">Retour à la liste</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}