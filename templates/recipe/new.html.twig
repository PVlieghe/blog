{% extends 'base.html.twig' %}

{% block title %}Nouvelle recette!{% endblock %}

{% block body %}
<div class="body">
    <div class="container mt-5">
        <div class="card custom-card bg-white rounded p-4">
            <h1 class="card-title">Créez votre recette</h1>
            
            <div class="card-body">
                {{ include('recipe/_form.html.twig') }}

                <a href="{{ path('app_recipe_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}

